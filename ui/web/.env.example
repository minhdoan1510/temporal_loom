# The URL of the Lending Claw backend API server.
# By default, Vite proxies requests to http://localhost:8080.
# Change this to point to a remote development server if needed.
# Example: https://dev-fin-lending-cs-tool.zalopay.vn
VITE_API_TARGET=http://localhost:8080
